import:
  - ~/.config/alacritty/themes/papercolor_dark.yaml

window:
  padding:
    x: 15
    y: 10

font:
  size: 5

key_bindings:
  - { key: V, mods: Control, action: Paste }
  - { key: C, mods: Control, action: Copy }
